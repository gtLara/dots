-- local telescope = require('telescope')
-- local telescope = require('telescope').setup({layout_strategy='vertical',layout_config={width=0.5}})

-- require("telescope").setup({
--     defaults = {
--         layout_strategy = 'vertical',
--         layout_config = {height = 20, widht=30},
--     },
-- })

require("telescope").setup({
    defaults = {
        layout_strategy = 'horizontal'
        -- layout_config = {height = 20, widht=30},
    },
})
